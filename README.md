# RAG Telegram Bot

Простой телеграм-бот с RAG (Retrieval-Augmented Generation) для ответов на вопросы по корпоративным документам.

## Функциональность

- 📄 Загрузка документов (PDF, DOCX, TXT)
- 🔍 Векторный поиск по документам (Qdrant)
- 🤖 Генерация ответов с помощью OpenAI GPT
- 💬 Ответы на вопросы сотрудников

## Архитектура

```
app/
├── ai/                    # AI модули
│   ├── llm.py            # OpenAI LLM
│   ├── vector_store.py   # Qdrant vector store
│   ├── rag.py            # RAG service
│   └── document_processor.py  # Обработка документов
├── bot/                   # Telegram bot
│   ├── bot.py            # Bot setup
│   └── handlers.py       # Message handlers
└── core/                  # Конфигурация
    ├── config.py         # Settings
    └── logging.py        # Logging
```

## Быстрый старт

### 1. Настройка окружения

```bash
cp .env.example .env
```

Заполните `.env`:
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `OPENAI_API_KEY` - API ключ OpenAI

### 2. Запуск через Docker

```bash
docker-compose up -d
```

### 3. Использование

1. Отправьте боту документ (PDF, DOCX, TXT)
2. Задайте вопрос по документу
3. Получите ответ на основе содержимого документов

## Команды бота

- `/start` - Начать работу
- `/help` - Справка
- `/ask <вопрос>` - Задать вопрос
- `/status` - Статус системы

## Зависимости

- Python 3.11+
- Qdrant (vector database)
- Redis (FSM storage)
- OpenAI API

## Разработка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Локальный запуск (требуются Qdrant и Redis)
python main.py
```
